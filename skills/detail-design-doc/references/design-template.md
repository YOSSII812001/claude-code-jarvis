# 詳細設計書テンプレート

> このテンプレートは detail-design-doc スキルが設計書を生成する際の骨格として使用される。
> 各セクションの詳細な記述ガイドは `section-guide.md` を参照。

---

```markdown
# 詳細設計書: {タイトル}

> 自動生成（detail-design-doc + Codex CLI）/ 生成日: {YYYY-MM-DD} / 品質スコア: {composite_grade}
> 入力ソース: {GitHub Issue #XXX / 仕様書: path/to/spec.md / テキスト要件}

---

## 1. 概要

### 1.1 背景
{この設計が必要になった背景・経緯}

### 1.2 目的
{この設計で達成すること}

### 1.3 スコープ
- 対象: {含まれるもの}
- 対象外: {明示的に含まないもの}

### 1.4 用語定義
| 用語 | 定義 |
|------|------|
| {term} | {definition} |

---

## 2. 要件定義

### 2.1 機能要件
| ID | 要件 | 優先度 | 受入条件 |
|----|------|--------|----------|
| FR-01 | {要件記述} | Must/Should/Could | {受入条件} |
| FR-02 | {要件記述} | Must/Should/Could | {受入条件} |

### 2.2 非機能要件
| ID | カテゴリ | 要件 | 基準値 |
|----|---------|------|--------|
| NFR-01 | パフォーマンス | {要件記述} | {具体的な数値基準} |
| NFR-02 | セキュリティ | {要件記述} | {具体的な基準} |
| NFR-03 | 可用性 | {要件記述} | {具体的な基準} |

---

## 3. アーキテクチャ設計

### 3.1 コンポーネント構成
```
{コンポーネント間の関係をASCII図で記述}

例:
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  Frontend   │────▶│  API Layer   │────▶│  Database   │
│  (React)    │◀────│  (Next.js)   │◀────│  (Supabase) │
└─────────────┘     └──────────────┘     └─────────────┘
```

### 3.2 データフロー
```
{主要なデータフローをASCII図で記述}

例:
User Input → Validation → API Call → DB Write → SSE Notify → UI Update
```

### 3.3 既存アーキテクチャとの整合性
{既存のアーキテクチャパターンとの整合性を説明}
- 既存パターンとの一致点: {具体的に}
- 既存パターンからの逸脱点と理由: {具体的に}

---

## 4. DB設計

### 4.1 テーブル定義
```sql
-- テーブル: {table_name}
-- 目的: {テーブルの目的}
CREATE TABLE {table_name} (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  {column_name} {TYPE} {constraints},
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  updated_at TIMESTAMPTZ DEFAULT now() NOT NULL
);

-- インデックス
CREATE INDEX idx_{table}_{column} ON {table_name}({column_name});
```

### 4.2 マイグレーション
```sql
-- マイグレーション: {YYYYMMDDHHMMSS}_{description}.sql
-- 注意: DROP COLUMN / DROP POLICY を含む場合は影響範囲を明記

{ALTER TABLE / CREATE TABLE 等}
```

### 4.3 RLS (Row Level Security)
```sql
-- RLS ポリシー
ALTER TABLE {table_name} ENABLE ROW LEVEL SECURITY;

CREATE POLICY "{policy_name}" ON {table_name}
  FOR {SELECT/INSERT/UPDATE/DELETE}
  USING ({condition});
```

### 4.4 既存スキーマとの整合性
{既存テーブルとのリレーション、型の整合性を明記}

> **TIMESTAMPTZ必須**: 新テーブル作成時は必ず `TIMESTAMPTZ` を使用すること（MEMORY.md教訓参照）

---

## 5. API設計

### 5.1 エンドポイント一覧
| メソッド | パス | 認証 | 概要 |
|---------|------|------|------|
| POST | `/api/v1/{resource}` | 要 | {概要} |
| GET | `/api/v1/{resource}/:id` | 要 | {概要} |

### 5.2 リクエスト・レスポンス
#### POST /api/v1/{resource}
**リクエスト**:
```typescript
interface CreateResourceRequest {
  field1: string;
  field2: number;
}
```

**レスポンス（成功: 201）**:
```typescript
interface CreateResourceResponse {
  id: string;
  field1: string;
  createdAt: string;
}
```

### 5.3 エラーレスポンス
| ステータス | エラーコード | 説明 | 対処 |
|-----------|------------|------|------|
| 400 | VALIDATION_ERROR | {説明} | {対処} |
| 401 | UNAUTHORIZED | {説明} | {対処} |
| 404 | NOT_FOUND | {説明} | {対処} |
| 500 | INTERNAL_ERROR | {説明} | {対処} |

---

## 6. UI設計

### 6.1 画面遷移
```
{画面遷移をASCII図で記述}

例:
一覧画面 ──[クリック]──▶ 詳細画面 ──[編集]──▶ 編集モーダル
    ▲                                              │
    └──────────────[保存/キャンセル]─────────────────┘
```

### 6.2 コンポーネント構造
```
{コンポーネントツリー}

例:
<PageContainer>
  ├── <Header title={...} />
  ├── <FilterBar filters={...} onChange={...} />
  ├── <DataTable>
  │   ├── <TableHeader />
  │   └── <TableRow /> × N
  └── <Pagination />
```

### 6.3 レスポンシブ対応
| ブレークポイント | レイアウト | 特記事項 |
|----------------|----------|----------|
| モバイル (~600px) | {レイアウト} | {特記} |
| タブレット (601-960px) | {レイアウト} | {特記} |
| デスクトップ (961px~) | {レイアウト} | {特記} |

---

## 7. 状態管理設計

### 7.1 状態分類
| 状態名 | 種別 | スコープ | 管理方法 |
|--------|------|---------|----------|
| {state} | サーバー状態 | グローバル | React Query / SWR |
| {state} | UI状態 | ローカル | useState |
| {state} | フォーム状態 | ローカル | React Hook Form |

### 7.2 状態遷移図
```
{状態遷移をASCII図で記述}

例:
idle ──[開始]──▶ loading ──[成功]──▶ success
                    │                    │
                    └──[失敗]──▶ error ──┘
                                  │
                                  └──[リトライ]──▶ loading
```

---

## 8. エラーハンドリング設計

### 8.1 エラー分類
| カテゴリ | 例 | ユーザー表示 | ログレベル | リトライ |
|---------|-----|------------|-----------|---------|
| バリデーション | 入力値不正 | フィールドエラー | warn | 不要 |
| 認証 | トークン期限切れ | ログイン画面遷移 | info | 不要 |
| ネットワーク | タイムアウト | リトライ案内 | error | 自動 |
| サーバー | 500エラー | 汎用エラー表示 | error | 手動 |

### 8.2 リトライ戦略
{リトライ回数、バックオフ、サーキットブレーカー等}

---

## 9. テスト戦略

### 9.1 テスト範囲
| テスト種別 | 対象 | ツール | カバレッジ目標 |
|-----------|------|--------|-------------|
| 単体テスト | ユーティリティ関数 | Jest/Vitest | {目標}% |
| コンポーネントテスト | UIコンポーネント | Testing Library | {目標}% |
| 統合テスト | API + DB | Supertest | 主要フロー |
| E2Eテスト | ユーザーフロー | Playwright | クリティカルパス |

### 9.2 テストケース
| ID | テスト内容 | 種別 | 優先度 |
|----|----------|------|--------|
| TC-01 | {テスト内容} | {種別} | 高/中/低 |
| TC-02 | {テスト内容} | {種別} | 高/中/低 |

---

## 10. セキュリティ考慮事項

### 10.1 認証・認可
{認証方式、認可ロジック、RLSポリシーとの関連}

### 10.2 入力検証
| 入力 | 検証内容 | サーバー側 | クライアント側 |
|------|---------|-----------|-------------|
| {input} | {validation} | {実装} | {実装} |

### 10.3 データ保護
{暗号化、マスキング、PII取り扱い等}

---

## 11. 実装ステップ

### Phase 1: 基盤（優先度: 高）
- [ ] Step 1.1: {タスク} — 対象: `{file_path}`
- [ ] Step 1.2: {タスク} — 対象: `{file_path}`

### Phase 2: コア機能（優先度: 高）
- [ ] Step 2.1: {タスク} — 対象: `{file_path}`
- [ ] Step 2.2: {タスク} — 対象: `{file_path}`

### Phase 3: 仕上げ（優先度: 中）
- [ ] Step 3.1: {タスク} — 対象: `{file_path}`
- [ ] Step 3.2: {タスク} — 対象: `{file_path}`

---

## 12. リスクと軽減策
| ID | リスク | 確率 | 影響度 | 軽減策 |
|----|--------|------|--------|--------|
| R-01 | {リスク} | 高/中/低 | 高/中/低 | {軽減策} |
| R-02 | {リスク} | 高/中/低 | 高/中/低 | {軽減策} |

---

## 13. 設計判断記録（ADR）

### ADR-001: {判断タイトル}
- **ステータス**: 承認 / 検討中 / 却下
- **コンテキスト**: {判断が必要になった背景}
- **選択肢**:
  - A) {選択肢A}
    - メリット: {メリット}
    - デメリット: {デメリット}
  - B) {選択肢B}
    - メリット: {メリット}
    - デメリット: {デメリット}
- **決定**: {選択した案}
- **根拠**: {なぜその案を選んだか}
- **影響**: {この決定による影響}

---

## 14. 影響範囲

### 14.1 変更対象ファイル
| ファイル | 変更種別 | 複雑度 | 概要 |
|---------|---------|--------|------|
| `{file_path}` | 新規/修正/削除 | 低/中/高 | {概要} |

### 14.2 コード現状検証結果
| ファイル | 最終変更 | 計画前提との乖離 |
|---------|---------|-----------------|
| `{file_path}` | {git log結果} | なし / {乖離内容} |

複雑度基準:
- **低**: 単純な値変更、1テーブル操作、既存パターンの踏襲
- **中**: 依存パラメータ変更、ファクトリ関数設計、複数コンポーネント連携
- **高**: 複数テーブル並列fetch、Promise.allSettled、複合ロジック分離、状態遷移設計

---

## 付録

### A. 用語集
| 用語 | 定義 |
|------|------|
| {term} | {definition} |

### B. 参考資料
- {参考資料・リンク}

### C. レビュー履歴
| レビュアー | 評価 | 主要指摘 |
|-----------|------|---------|
| Reviewer A (技術的実現可能性) | {grade} | {要約} |
| Reviewer B (アーキテクチャ適合性) | {grade} | {要約} |
| Reviewer C (Devil's Advocate) | {grade} | {要約} |
```
